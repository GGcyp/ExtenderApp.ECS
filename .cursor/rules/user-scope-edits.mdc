---
description: 点名范围编辑、修改前方案、中文注释与中文错误信息
alwaysApply: true
---

# 编辑范围（用户授权）

- **默认**：只修改用户在当次请求中**明确列出的文件或路径**（例如「改 `Foo.cs`」或「只动 `src/X/`」）。
- **禁止**：在未获用户**另行许可**时，对未点名的文件做任何改动，包括顺手重构、格式化、修无关告警、改依赖或「顺便」修复其他问题。
- **若需扩大范围**：先向用户说明要动哪些额外文件及原因，**得到明确同意后再改**。
- **修改方案**：当需要对某个文件/片段做非纯注释修改（包括重构、逻辑调整、API 设计等）时，先用文字列出「修改点清单 + 大致方案」，待用户确认后再真正落地改动。
- **损坏注释**：如果发现代码中的注释文本为乱码或一串 `?`（如 `????????`），视为损坏注释，允许在不更改代码语义的前提下**直接重写为简体中文注释**并告知用户修改范围。

# 文档与语言（生成与修改代码时）

- **同步中文注释**：在本仓库内**新生成或实质性改动的代码**上，应同步补充**简体中文** XML 文档注释（`///`）或等价块注释；与现有文件风格一致时优先用 `///`。
- **覆盖范围**：对本次改动涉及的**公开/内部 API**，为**属性、字段、方法/函数、构造函数、类型**等补充注释，说明用途与语义（避免堆砌实现细节）；`private` 成员若对理解关键路径有必要，同样补充简短中文说明。
- **错误信息中文**：面向开发者或运行时的**错误文案**使用简体中文，例如 `throw new ...` 的消息、`InvalidOperationException` / `ArgumentException` 等说明、日志中与错误相关的用户可读描述；与外部协议或第三方 API 强制英文的字段除外。
